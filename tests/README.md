# ClinicPulse AI Tests

This directory will contain integration tests that simulate a patient flow conversation with the agent graph. Once the agent implementation is ready, run:

```bash
python -m tests.test_agent
```

to exercise intake → triage → briefing end-to-end.
